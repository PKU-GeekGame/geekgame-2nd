本题主要就是求解一个整数变量（并且每个变量都在可打印的ASCII字符区间内）的线性方程组，其中flag1是静定的，后两个flag是欠定的。
flag1很简单地用MATLAB求解Ax=B即可。
而flag2，我用了MATLAB的intlinprog函数（该函数用于整数线性规划），大致求出了几个字符的可能值，然后大胆猜测，假设了其中的几个取值，得到静定的方程组如果解出来是有意义的句子，就得到了正确答案。